name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # Allows this workflow to be called from other workflows (publish.yml runs
  # it as a gate before uploading to PyPI, since tag pushes don't match the
  # branch filters above).
  workflow_call:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python-version }}
          enable-cache: true
          cache-dependency-glob: "pyproject.toml"

      - name: Run tests
        run: >-
          uv run --extra dev python -m pytest tests/
          -o addopts="-v --strict-markers --cov=pyobjtojson --cov-report=term-missing"

      - name: Type check
        run: uv run --extra dev mypy pyobjtojson/
